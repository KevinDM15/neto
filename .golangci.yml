version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gofmt
    - goimports
    - gosimple
    - ineffassign
    - typecheck

settings:
  goimports:
    local-prefixes:
      - github.com/neto-app/neto

exclusions:
  paths:
    - vendor
